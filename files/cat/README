ft_cat_lock [FILE ...]

This program is a simple version of cat that locks files it reads, no options needed, it just print files, it takes 1 or more files as argument, if not, it read standard input.

read is allowed.
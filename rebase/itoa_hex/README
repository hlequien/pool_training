char *ft_itoa_hex(int n);

This function takes an integer as argument and returns a string that contains the value of n in hexadecimal.

Positives numbers must be provided, if not, the behavior is undefined.